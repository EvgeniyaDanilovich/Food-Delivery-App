import React from 'react';
import {Text} from 'react-native';

function DishesScreen(): React.JSX.Element {
  return <Text>You can see the whole list of dishes.</Text>;
}

export default DishesScreen;
