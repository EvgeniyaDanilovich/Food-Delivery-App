import React from 'react';
import {Text} from 'react-native';

function RestaurantScreen(): React.JSX.Element {
  return <Text>You can see the description of selected restaurant.</Text>;
}

export default RestaurantScreen;
